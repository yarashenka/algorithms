var arr = [3, 4, 5, 6, 7, 2, 9];
var tmp;
for (i=0, i<arr.length - 1, i++) {
  if (arr[i] < arr[i+1]) {
  }  else { 
       tmp = arr[i];
       arr[i] = arr[i+1];
       arr[i+1] = tmp;
  }
}       
console.log(arr);
         
        
    
 
